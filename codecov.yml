coverage:
  status:
    project:
      default:
        target: auto
        threshold: 5%
    patch:
      default:
        target: auto

comment:
  layout: 'header, diff, flags, files'
  behavior: default
  require_changes: false
